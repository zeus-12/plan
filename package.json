{
  "name": "plan",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @plan/web dev",
    "dev:desktop": "pnpm --filter @plan/desktop dev",
    "build:web": "pnpm --filter @plan/web build",
    "build:desktop": "pnpm --filter @plan/desktop build",
    "validate": "npm run typecheck && npm test && npm run lint && npm run format:check",
    "typecheck": "npm --prefix shared run typecheck && npm --prefix apps/web run typecheck && npm --prefix apps/desktop run typecheck",
    "lint": "npm --prefix apps/web run lint",
    "format": "npm --prefix apps/web run format && npm --prefix apps/desktop run format && npm --prefix shared run format",
    "format:check": "npm --prefix apps/web run format:check && npm --prefix apps/desktop run format:check && npm --prefix shared run format:check",
    "release": "bash scripts/release.sh",
    "test": "npm --prefix apps/desktop run test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "node-pty"
    ]
  }
}
