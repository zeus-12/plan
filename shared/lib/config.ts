/**
 * Where the `@plan/web` frontend is deployed. The desktop app links out to it
 * (e.g. "Share as doc" opens `${FRONTEND_URL}/doc#c=…`). Override per-build via
 * the env var if a different host is ever needed.
 */
export const FRONTEND_URL = "https://plan.vishnuu.com";
