{
  "name": "@plan/control-plan",
  "private": true,
  "type": "module",
  "description": "CDP driver and performance suite for the desktop app.",
  "scripts": {
    "test": "tsx test.ts",
    "cli": "tsx cli.ts",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write . --ignore-path ../../.prettierignore",
    "format:check": "prettier --check . --ignore-path ../../.prettierignore"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "prettier": "^3.8.3",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
